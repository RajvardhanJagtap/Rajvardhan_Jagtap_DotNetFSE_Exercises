// Shared student model for the template-driven and reactive forms.
export interface Student {
  id?: number;
  name: string;
  email: string;
  phone: string;
  course: string;
  gender: string;
  address: string;
}
